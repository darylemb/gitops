apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: trust-manager-resources
  namespace: flux-system
spec:
  interval: 1h
  path: ./infrastructure/trust-manager/resources
  prune: true
  wait: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: trust-manager